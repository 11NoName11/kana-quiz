// Service worker for Kana Quiz app
// The Workbox manifest will be injected here by the build process
// Uncomment below if using Workbox with proper configuration:
// import { precacheAndRoute } from 'workbox-precaching';
// precacheAndRoute(self.__WB_MANIFEST);

if (typeof self !== 'undefined') {
  // Service worker context
  // Add service worker functionality here if needed
}

